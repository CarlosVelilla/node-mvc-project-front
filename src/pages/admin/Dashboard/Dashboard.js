import React from "react";

import withLayoutClient from "../../../hoc/withLayoutAdmin";

const Dashboard = () => {
  return (
    <>
      <h1>Dashboard</h1>
    </>
  );
};

export default withLayoutClient(Dashboard);
